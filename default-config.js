(function() {
    "use strict";

    define({
        serverAddress: 'http://' + window.location.host + ':5000',
        // targetExperiment: 'test', // deprecated
        mainDiv: "#main-div",
        debug: {
            managerfsm: false
        }
    });

}());
