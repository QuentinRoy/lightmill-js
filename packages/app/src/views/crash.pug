.lightmill-base-app.lightmill-base-app-crash

  .lightmill-banner
    h1
      | Something went wrong

    div
      | Please call your experimenter.
    
  .lightmill-details
    h3
      | Run #{run ? run.id : 'unknown'}
    h4.lightmill-message= message
    if stack
      pre.lightmill-stack= stack
  
  a.lightmill-details-button(href='#')
    | show details
