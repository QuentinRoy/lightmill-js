(function () {
    "use strict";

    define({
        SERVER_ADDRESS: 'http://' + window.location.host + ':5000',
        TARGET_EXPERIMENT: 'prenomode',
        DEBUG: {
            managerfsm: false
        }
    });

}());